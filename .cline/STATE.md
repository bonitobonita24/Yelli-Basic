# Project State — Yelli
# Auto-generated. Never edit manually.
# Updated: 2026-06-02 by CLAUDE_CODE (Opus 4.7 Architect + Sonnet 4.6 Executor, V32 R1)

PHASE:        Phase 4 Part 7 complete (governance tools + Docker Compose stacks + SocratiCode artifacts)
LAST_DONE:    Wrote 33 files on scaffold/part-7 squash-merged to main as ed05599 (895c9bb..ed05599). Part 7 delivers the full infrastructure scaffold for Yelli's dev/stage/prod environments. tools/ (4 Node ESM .mjs governance tools wired to pnpm scripts): validate-inputs.mjs (custom YAML parser + JSON Schema validator, no external deps — ajv/yaml not in monorepo; handles YAML union types like ["integer","null"], block sequences, lazy array init); check-env.mjs (env file checker, declares required keys per env: dev needs 10 vars, staging/prod add APP_DOMAIN, TRAEFIK_NETWORK, NEXT_PUBLIC_TURNSTILE_SITE_KEY, TURNSTILE_SECRET_KEY, SMTP_*); check-product-sync.mjs (Rule 20 private-tag leak scan across 5 governance docs + soft tech-stack alignment check); hydration-lint.mjs (warn-only SSR pattern scanner — Date.now/Math.random/typeof window/localStorage at module scope). All 4 tools exit 0 on current state. deploy/compose/ — full V31 Rule 5 split-by-service-group pattern across 3 environments: dev/ (7 files: db postgres+pgbouncer DEFINES yelli_dev_network bridge; cache valkey:7-alpine; storage minio:latest dual ports 9000/9001; infra mailhog SMTP+UI; pgadmin pgadmin4:latest with mounted pgadmin-servers.json; app builds yelli:dev-latest from apps/yelli/Dockerfile with --build flag; pgadmin-servers.json pre-configured); stage/ (6 files — same as dev minus mailhog; app uses image-only NO build key + Traefik V27 labels: traefik.enable, Host(`${APP_DOMAIN}`), entrypoints=websecure, certresolver=letsEncrypt, loadbalancer port=3000; dual networks app_network + external proxy; no host port — Traefik routes via internal Docker network); prod/ (7 files — same as stage + docker-compose.cloudflared.yml migration sidecar carrying forward project-root compose.yaml pattern for yelli-maes.powerbyte.app live deploy where Traefik docker-provider is broken on srv709899; default image tag :prod floating semver per inputs.yml deploy.cloud.prod.tag_scheme). All compose files: env_file: ../../../.env.${ENV} (THREE levels up to project root — initial templates used `../../` which incorrectly resolved to deploy/.env.dev; fixed across 19 occurrences), container_name=${COMPOSE_PROJECT_NAME}_<svc>, named volumes ${COMPOSE_PROJECT_NAME}_<svc>_data, restart unless-stopped, healthchecks on postgres/valkey/minio/app, NO inline credentials. deploy/compose/start.sh (multi-`-f` single-project pattern — DIVERGES from V31 template which iterates files sequentially as separate compose projects, breaking cross-file depends_on; locked in lessons.md): `docker compose -p yelli_$ENV --env-file $(pwd)/.env.$ENV -f db.yml -f cache.yml -f storage.yml -f pgadmin.yml [-f infra.yml dev-only] -f app.yml [-f cloudflared.yml prod-only] $CMD`. Special-cases dev `up` to add `--build` (matches inputs.yml docker.dev_build=true). Validates ENV in {dev,stage,prod}, exits 1 if .env.$ENV missing. deploy/compose/push.sh (manual image promotion: dev (build + dev-stack test + push :dev-latest + :dev-sha-{hash}) → staging (re-tag dev-latest as :staging-latest + :staging-sha-{hash} + push) → prod (re-tag staging-latest as :prod + :prod-sha-{hash} + push); guards on docker.publish: true, docker login state, DOCKERHUB_USERNAME env). COMMANDS.md (project root, 242L, 13 sections per V31 template: Docker start/stop/rebuild, clean/reset, image build+push, database, testing, code quality, governance tools, git workflow, AI agent triggers, dev URLs, credentials, utilities, common full workflow). .socraticodecontextartifacts.json (4 entries: database-schema → packages/db/prisma/schema.prisma, implementation-map → docs/IMPLEMENTATION_MAP.md, decisions-log → docs/DECISIONS_LOG.md, product-definition → docs/PRODUCT.md — Phase 2.6 design-system entry not present because design-system/MASTER.md was never generated; entry was reserved per template but not added). package.json (+4 tools:* npm scripts: validate-inputs, check-env, check-product-sync, hydration-lint). .env.dev (+STORAGE_PORT=46841 + SMTP_UI_PORT=46844 + MINIO_ROOT_USER=yelli-dev-0e550979ae360b3cf3d872 + MINIO_ROOT_PASSWORD=<matches STORAGE_SECRET_KEY> + APP_DOMAIN=localhost — Bootstrap Step 18 omitted discrete port vars in favor of STORAGE_ENDPOINT URL form, but compose port-mappings need bare port numbers). .env.staging + .env.prod (+structural vars STORAGE_PORT, SMTP_UI_PORT, MINIO_ROOT_USER ⏳, MINIO_ROOT_PASSWORD ⏳, TRAEFIK_NETWORK=proxy, APP_DOMAIN, APP_IMAGE_TAG; prod adds CF_TUNNEL_TOKEN ⏳). Runtime verification PASSED: bash deploy/compose/start.sh dev up -d → postgres+valkey+minio+pgadmin+mailhog all healthy; pgbouncer restarts then stabilizes; app health: starting (yelli:dev-latest exists from prior Phase 4 Part 5 build); bash deploy/compose/start.sh dev down → clean teardown. Governance docs updated: CHANGELOG_AI.md (172→215L, Part 7 entry prepended with 7-dispatch ledger), IMPLEMENTATION_MAP.md (173→181L, Part 7 ✅ block + Phase Status updated to 7/8), lessons.md (183→197L, 2 typed entries: 🔴 gotcha compose env_file 3-level path resolution + 🟤 decision start.sh multi-`-f` single-project pattern), agent-log.md (18→19L, 1-line completion record).
NEXT:         Phase 4 Part 8 — open `.cline/tasks/phase4-part8.md` in a NEW Claude Code session per Rule 24. Trigger: "Start Part 8". Standard Part 8 territory: CI workflow (.github/workflows/ci.yml + docker-publish.yml conditional on docker.publish=true), README.md generation (only if Phase 8 confirms PRODUCT.md fully implemented — likely deferred), MANIFEST.txt (every file generated across Parts 1-8), .github/skills/spec-driven-core/SKILL.md verification, SocratiCode initial index trigger (codebase_index after Part 8). Read the Part 8 task file first to confirm exact scope.
BLOCKERS:     none for Part 8. Phase 5 will still block on unfilled CREDENTIALS.md ⏳ fields: MINIO_ROOT_USER staging/prod, MINIO_ROOT_PASSWORD staging/prod, CF_TUNNEL_TOKEN prod, GitHub PAT, Docker Hub token, SMTP staging/prod, Turnstile prod keys, third-party API keys. .env.example (committed) needs to be regenerated by Part 8 or by a follow-up Feature Update to mirror the new vars added in Part 7 (STORAGE_PORT, SMTP_UI_PORT, MINIO_ROOT_USER, MINIO_ROOT_PASSWORD, APP_DOMAIN, TRAEFIK_NETWORK, APP_IMAGE_TAG, DOCKERHUB_USERNAME, IMAGE_NAME, CF_TUNNEL_TOKEN). Phase 6 must still runtime-verify the FULL combined stack including app reachability + Traefik routing on first staging deployment.
GIT_BRANCH:   main (scaffold/part-7 squash-merged as ed05599, branch deleted; not pushed to origin)
GIT_TAG:      pre-spec-driven-adoption-20260531 (on main pre-rewrite — untouched)
PORTS:        UNCHANGED — base=46838, db=46838, pgbouncer=46839, redis=46840, minio=46841, minio_console=46842, mailhog=46843, mailhog_ui=46844, pgadmin=46845, app=46848, worker=46849, prisma_studio=46858.
MIGRATION:    brownfield=true. Parts 1+2+3+4+5+7 done. Part 6 skipped (no native mobile — PWA from Part 5 covers all declared mobile use cases). Live deploy at yelli-maes.powerbyte.app stays on prior commit a251049 (vanilla edition) until rewrite finishes + manual Komodo redeploy after Phase 6 staging validation.
LIVE_DEPLOY:  yelli-maes.powerbyte.app (vanilla edition operational; not auto-redeploying during Phase 4)
MODELS:
  planning:   claude-code (Opus 4.7 — Architect ONLY per V32 R1)
  execution:  claude-sonnet-4-6 (via Claude Code — all file writes; D6a/b/c fix dispatches handled compose path bugs + start.sh refactor without Opus executor escalation)
  governance: gemini-2.5-flash-lite
LINES_TOUCHED: ~1500 this Part (4 tools ~440L + 20 compose YAML ~600L + 3 pgadmin-servers.json ~45L + start.sh 76L + push.sh 119L + COMMANDS.md 242L + .socraticodecontextartifacts.json 24L + 4 governance doc appends ~66L + 4 env file appends ~30L + package.json 4-line addition)
CHECKPOINT_TYPE: full
FILES_TOUCHED:
  tools/:
    - validate-inputs.mjs (~160L — custom YAML parser, built-ins only)
    - check-env.mjs (~80L)
    - check-product-sync.mjs (~110L)
    - hydration-lint.mjs (~90L)
  deploy/compose/dev/:
    - docker-compose.{db,cache,storage,infra,pgadmin,app}.yml (6 files, ~187L total)
    - pgadmin-servers.json (~14L)
  deploy/compose/stage/:
    - docker-compose.{db,cache,storage,pgadmin,app}.yml (5 files, ~180L total)
    - pgadmin-servers.json (~14L)
  deploy/compose/prod/:
    - docker-compose.{db,cache,storage,pgadmin,app,cloudflared}.yml (6 files, ~210L total)
    - pgadmin-servers.json (~14L)
  deploy/compose/:
    - start.sh (76L, multi-`-f` pattern, chmod +x)
    - push.sh (119L, dev→stage→prod promotion, chmod +x)
  root:
    - COMMANDS.md (242L, NEW)
    - .socraticodecontextartifacts.json (24L, NEW — 4 entries)
    - package.json (modified — 4 tools:* scripts appended)
    - .env.dev (modified — 5 vars appended)
    - .env.staging (modified — 6 vars appended)
    - .env.prod (modified — 7 vars appended including CF_TUNNEL_TOKEN ⏳)
  governance:
    - docs/CHANGELOG_AI.md (Part 7 entry prepended)
    - docs/IMPLEMENTATION_MAP.md (Part 7 ✅ block + Phase Status 7/8)
    - .cline/memory/lessons.md (+2 typed entries)
    - .cline/memory/agent-log.md (+1 line)
    - .cline/STATE.md (this checkpoint — Opus exception per V32 R1)
TIER_CLASSIFICATION: 2 — moderate (Part 7 is governance/infra scaffolding, no source code generation; ~33 files but each is small + standalone YAML/JSON/.mjs; decomposed into 7 Sonnet dispatches: D1 tools/ / D2 dev compose / D3 stage compose / D4 prod compose+cloudflared / D5 scripts+commands+SocratiCode / D6a start.sh --env-file fix / D6b compose env_file path fix / D6c env var alignment + start.sh multi-`-f` refactor / D7 governance / D8 commit+merge — average dispatch ≤300L per V32 R2; well within budget)
DISPATCH_LEDGER (Phase 4 Part 7):
  D1:    git checkout -b scaffold/part-7 + tools/{validate-inputs.mjs, check-env.mjs, check-product-sync.mjs, hydration-lint.mjs} + 4 pnpm tools:* scripts in package.json (initial blockers: missing yaml + ajv deps in monorepo → resolved via custom YAML parser using built-ins; 3 iterations to handle YAML union types and lazy array init)
  D2:    deploy/compose/dev/ — 7 files (db+pgbouncer+cache+storage+infra+pgadmin+app + pgadmin-servers.json); dev app uses build: context ../../.. + image yelli:dev-latest
  D3:    deploy/compose/stage/ — 6 files; V27 Traefik labels on app (NO build, NO host port, dual networks app_network + proxy external, certresolver letsEncrypt)
  D4:    deploy/compose/prod/ — 7 files; same Traefik pattern as stage + docker-compose.cloudflared.yml migration sidecar carrying forward project-root compose.yaml pattern (cloudflare/cloudflared:latest, TUNNEL_TOKEN from .env.prod, reaches yelli_prod_app:3000 via Docker DNS — used INSTEAD of Traefik labels on srv709899 where docker-provider is broken)
  D5:    start.sh (initial sequential per-file pattern; 76L) + push.sh (119L dev/staging/prod promotion gate) + COMMANDS.md (242L) + .socraticodecontextartifacts.json (4 entries)
  D6a:   start.sh --env-file fix — initial start.sh missed --env-file flag at CLI level; compose YAML interpolations like ${DB_PORT} resolved to blank without it; fix: pass --env-file $(pwd)/.env.$ENV to every docker compose invocation
  D6b:   compose env_file path fix — V31 template `../../.env.${ENV}` walks 2 levels up; our layout deploy/compose/{env}/ is 3 levels deep → fix: ../../../.env.${ENV} across 19 occurrences in 16 compose files; locked as 🔴 gotcha in lessons.md
  D6c:   env var alignment (.env.{dev,staging,prod} appended STORAGE_PORT/SMTP_UI_PORT/MINIO_ROOT_USER/MINIO_ROOT_PASSWORD/APP_DOMAIN/TRAEFIK_NETWORK/APP_IMAGE_TAG/CF_TUNNEL_TOKEN as appropriate) + start.sh multi-`-f` refactor (collapsed sequential per-file pattern into single `docker compose -p yelli_$ENV -f db.yml -f cache.yml -f storage.yml -f pgadmin.yml [-f infra.yml] -f app.yml [-f cloudflared.yml] $CMD` — cross-file depends_on now works because single project, single network; --build only added on dev up); RUNTIME VERIFY: stack up→postgres/valkey/minio/pgadmin/mailhog healthy + pgbouncer transient + app starting; clean down — captured as 🟤 decision lessons.md
  D7:    governance — CHANGELOG_AI.md prepend (Part 7 entry) + IMPLEMENTATION_MAP.md (Part 7 ✅ + Phase Status 7/8) + lessons.md (+2 typed entries: 🔴 compose env_file path + 🟤 start.sh multi-`-f`) + agent-log.md (+1 line)
  D8:    git add -A → commit on scaffold/part-7 (35a8e01) → checkout main → merge --squash → commit on main (ed05599) → branch -D scaffold/part-7 (force after squash); not pushed to origin
NEXT_DISPATCH: Human opens fresh Claude Code session → "Start Part 8" from .cline/tasks/phase4-part8.md (per Rule 24 fresh-context discipline). Expected scope: .github/workflows/{ci.yml, docker-publish.yml conditional on docker.publish=true} + MANIFEST.txt (full Phase 4 file inventory) + SocratiCode codebase_index trigger + .github/skills/spec-driven-core/SKILL.md verification + final governance Sync if Phase 8 is part of Part 8 (or deferred). After Part 8 squash-merge: STOP. Human says "Start Phase 5" in a NEW session for the 9-command validation suite. Pre-flight Part 8: STATE.md (this file) + inputs.yml + relevant PRODUCT.md sections + lessons.md (the new 🔴 compose env_file path + 🟤 start.sh multi-`-f` gotchas are critical reads for any future deploy/compose work).
