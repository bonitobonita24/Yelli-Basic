export default function HomePage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col items-center justify-center gap-4 px-6 text-center">
      <h1 className="text-4xl font-semibold tracking-tight text-text-primary">Yelli</h1>
      <p className="text-text-secondary">
        Dual-mode peer-to-peer calling. App shell scaffolded — screens land as the Phase 3.3
        validated flows are wired to the backend.
      </p>
    </main>
  );
}
