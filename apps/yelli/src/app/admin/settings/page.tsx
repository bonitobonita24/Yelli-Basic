import ScreenTenantSettings from '@/components/screens/ScreenTenantSettings';

/** Page 18 — Org settings (W1b mounts the screen behind the admin gate). */
export default function AdminSettingsPage() {
  return <ScreenTenantSettings />;
}
