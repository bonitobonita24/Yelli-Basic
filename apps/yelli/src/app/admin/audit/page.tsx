import ScreenAdminAudit from '@/components/screens/ScreenAdminAudit';

/** Flow H — audit log (W1b mounts the S3d-ported screen behind the admin gate). */
export default function AdminAuditPage() {
  return <ScreenAdminAudit />;
}
