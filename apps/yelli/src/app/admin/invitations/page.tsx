import ScreenAdminInvitations from '@/components/screens/ScreenAdminInvitations';

/** Flow F — invitations (W1b mounts the S3c-ported screen behind the admin gate). */
export default function AdminInvitationsPage() {
  return <ScreenAdminInvitations />;
}
