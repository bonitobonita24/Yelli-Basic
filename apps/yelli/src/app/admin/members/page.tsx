import ScreenAdminMembers from '@/components/screens/ScreenAdminMembers';

/** Flow G — device directory / members (W1b mounts the S3c-ported screen behind the admin gate). */
export default function AdminMembersPage() {
  return <ScreenAdminMembers />;
}
