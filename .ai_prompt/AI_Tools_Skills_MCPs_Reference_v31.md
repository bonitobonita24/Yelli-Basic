# Spec-Driven Platform V31 — AI Tools, Skills & MCPs Reference

> **Purpose:** Complete reference of every AI tool, skill, MCP server, and model used in the
> Spec-Driven Platform framework. Covers what each tool is, why it exists, when it activates,
> how to install it, and how it connects to PRODUCT.md and app structure.
>
> **Who reads this:** Bonito (human) and agents reading project knowledge.
> **Agents:** Read Section 6 (Model Routing) and Section 7 (How Tools Connect to PRODUCT.md)
> before making any tool selection decisions.
>
> Last updated: V32.9 · Powerbyte IT Solutions · Lipa City, Philippines

---

## Table of Contents

1. [Agent Stack — The 6 Agents](#1-agent-stack)
2. [MCP Servers — Always Active](#2-mcp-servers)
3. [Skills & Plugins — Contextual Load](#3-skills--plugins)
4. [Infrastructure Tools — Phase-Generated](#4-infrastructure-tools)
5. [Model Routing — Which AI Does What](#5-model-routing)
6. [How Tools Connect to PRODUCT.md](#6-how-tools-connect-to-productmd)
7. [Complete Install Checklist](#7-complete-install-checklist)

---

## 1. Agent Stack

Six agents form the complete automation layer. Each has a hard phase boundary (Rule H4).

| Agent | Phase Boundary | What It Does | How It Loads |
|---|---|---|---|
| **Claude Code** | ALL phases (primary executor) | Primary agent. **Opus 4.6 = Architect ONLY** — planning, decomposition, review, STATE.md checkpoint writes. NEVER calls Edit/Write on project files (V32 Zero Opus Execution rule R1). **Sonnet 4.6 = Sole Executor** — all file writes, implementations, tests, commits via `Agent(model: "sonnet")` subagents. Dispatch gate: `wc -l` ≤ 500 total lines per task; files > 300 lines need explicit line ranges (V32 R2/R3). Failure protocol: 3 re-decomposition attempts → defer to next session, NEVER escalate to Opus execution (V32 R4). See `memory-governance.md` §1+§4 for the full V32 dispatch rules. Handles Bootstrap through Phase 8, Feature Updates, Resume, Gov Sync. **Historical note:** Step 2.5/2.5b (30K token budget gate + Opus executor escalation) was the V31.2-era model and is REMOVED in V32. | Auto-loads compact CLAUDE.md (~200 lines — the ONLY auto-loaded file, V32.7). Reads `.ai_prompt/` on-demand per task. Reads `.ai_prompt/memory-governance.md` at every phase pre-flight via explicit Read command in phases.md pre-flight step |
| **Cline** | ⚠ DEPRECATED (V31) — do not use | .clinerules still generated by Bootstrap but unused. Claude Code handles all work Cline used to handle. Kept in framework for historical reference only. | N/A — deprecated |
| **Copilot** | Inline autocomplete + PR review | Always-on autocomplete while you type. Fallback if primary agent hits unresolvable error | VS Code extension — always on |
| **SpecStory** | Passive (all phases) | Captures every Claude Code session to `.specstory/history/`. Powers Governance Sync attribution | VS Code extension — zero config |
| **SocratiCode** | Phase 4 Part 8 onward (after scaffold) | Semantic codebase search. 61% less context, 84% fewer tool calls, 37x faster than grep | MCP server via npx — Docker required |
| **code-review-graph** | Phase 7 (Feature Updates) | Structural blast-radius: what breaks if I change this? Tree-sitter graph + SQLite | Claude Code plugin — one-time per machine |

> **Note — Planning Assistant (NEW V31 role expansion):**
> The Planning Assistant chat runs on **claude.ai** (external to the project) using `Product_md_Planning_Assistant_v31.md` as its system prompt. It produces PRODUCT.md during Phase 2 interview and generates an ephemeral HTML clickable mockup during Phase 2.8 (V31). It is NOT counted as one of the 6 in-project agents because it never reads or writes any project file — the human copies PRODUCT.md into the project after the Planning Assistant chat concludes.

---

## 2. MCP Servers

MCP servers run alongside the agents as persistent local services. They extend what agents can do.

### 2.1 SocratiCode
```
Repo:     (runs via npx socraticode)
Type:     MCP server — semantic codebase intelligence
Added:    V10
Install:  Runs automatically via .vscode/mcp.json entry (Bootstrap Step 10)
          Requires: Docker Desktop running on Windows (WSL2 accesses natively)
```

**What it does:** Hybrid semantic + keyword search across the entire codebase. Builds an AST-aware chunk index using Qdrant (vector DB) + Ollama (embeddings), both running in Docker. On first use it auto-pulls both images (~5 min one-time).

**21 tools including:**
- `codebase_search` — semantic search for concepts ("where is auth handled?")
- `codebase_context_search` — retrieves pre-built context artifacts (schema, docs)
- `codebase_index` — build or rebuild the full index
- `codebase_update` — incremental update after file changes
- `polyglot_dependency_graph` — cross-language dependency map

**When agents use it:**
- Phase 4 Part 8: initial index built after scaffold completes
- Phase 7 Step 2: `codebase_search` before touching any file (Rule 17)
- Phase 7 Step 10: `codebase_update` after implementation

**`vscode/mcp.json` entry (Bootstrap Step 10 generates this):**
```json
{
  "servers": {
    "socraticode": {
      "command": "npx",
      "args": ["-y", "socraticode"]
    }
  }
}
```

---

### 2.2 code-review-graph
```
Repo:     github.com/tirth8205/code-review-graph
Type:     MCP server — structural blast-radius analysis
Added:    V13
Install:  claude plugin add tirth8205/code-review-graph  (one-time per machine)
          Requires: Python 3.10+, uv package manager
```

**What it does:** Builds a persistent Tree-sitter graph of the codebase stored in SQLite with NetworkX traversal. Answers "what breaks if I change this?" — structural impact, not semantic search. Supports 12 languages.

**8 tools including:**
- `get_impact_radius_tool` — blast radius: what files/functions break if this changes
- `get_review_context_tool` — token-optimized context bundle (changed files + impacted)
- `query_graph_tool` — callers, callees, tests, imports for any symbol
- `build_or_update_graph_tool` — full or incremental graph build
- `semantic_search_nodes_tool` — search by name or semantic similarity

**When agents use it:**
- Phase 7 Step 3: `get_impact_radius_tool` before every Feature Update (Phase 7 blast-radius check)
- Two-stage review (Rule 25): `get_review_context_tool` for scope-limited review

**Per-project setup (after machine install):**
```bash
# Run once per project after Phase 4 completes
code-review-graph build
```

**How it complements SocratiCode:**
| Question | Tool |
|---|---|
| "Where is auth implemented?" | SocratiCode `codebase_search` |
| "What breaks if I change this entity?" | code-review-graph `get_impact_radius_tool` |
| "What tests cover this function?" | code-review-graph `query_graph_tool` |
| "Show me the schema structure" | SocratiCode `codebase_context_search` |

---

### 2.3 Context7
```
URL:      https://claude.com/plugins/context7
Type:     MCP server — live library documentation
Added:    V23 (Rule 30)
Install:  Automatically wired in .vscode/mcp.json by Bootstrap Step 10
```

**What it does:** Fetches current, version-specific documentation from official library source repos before Claude writes code using that library. Prevents deprecated API hallucinations — critical because Next.js, Prisma, Auth.js, tRPC all change APIs between versions.

**When agents use it:**
- Rule 30: any Claude Code task involving an external library gets "use context7" appended
- Covered libraries: Next.js, Prisma, Auth.js, tRPC, Valkey, shadcn/ui, BullMQ, Expo, WatermelonDB
- Before writing any route handler, Prisma query, tRPC procedure, or Auth.js config

**`vscode/mcp.json` entry (Bootstrap Step 10 generates this):**
```json
{
  "servers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"]
    }
  }
}
```

**Usage pattern:**
```
"Add a Prisma migration for the new Booking entity. use context7"
```

---

### 2.4 shadcn/ui MCP Server (NEW V29)
```
URL:      https://ui.shadcn.com/docs/mcp
Type:     MCP server — component search, browse, and install
Added:    V29
Install:  Automatically wired in .vscode/mcp.json by Bootstrap Step 10
          Command: npx shadcn@latest mcp
```

**What it does:** Enables AI agents to search, browse, and install shadcn/ui components, blocks, and templates from any configured registry using natural language. Instead of agents manually running `npx shadcn add [component]`, they describe what they need and the MCP server handles resolution and installation.

**When agents use it:**
- Phase 4 Part 5: initial UI scaffold — install all components needed by PRODUCT.md
- Phase 7 Feature Updates: install new components as features are added
- Any task involving UI — agents query the MCP server before writing custom components

**Example prompts:**
```
"Show me all available form components"
"Add button, dialog, card, and sidebar to my project"
"Find a login form from the shadcn registry"
"Install chart and data-table components"
```

**Supported registries:**
- shadcn/ui (default) — all official components and blocks
- Community registries configured in `components.json`
- Private registries (company internal component libraries)

**`.vscode/mcp.json` entry (Bootstrap Step 10 generates this):**
```json
{
  "servers": {
    "shadcn": {
      "command": "npx",
      "args": ["-y", "shadcn@latest", "mcp"]
    }
  }
}
```

No API key needed. Free. MIT licensed.

---

### 2.5 Shadcn Studio Pro MCP Server (NEW V32.11)
```
URL:      https://shadcnstudio.com/docs/getting-started/shadcn-studio-mcp-server
Type:     MCP server — phase-aware DESIGN GENERATOR built on shadcn/ui (Pro account)
Added:    V32.11
Scope:    user-global — registered once on the build machine; auto-available in every
          framework app session. NOT project-wired, NOT committed to any app repo.
Auth:     API key + account email passed as CLI args at registration; the key lives only in
          ~/.claude.json on the build machine (single-user local). Account creds + the
          registry license key (for the shadcn CLI @ss-blocks/* registry) are SOPS+age
          encrypted at Server-Setups/secrets/shadcn-studio.enc.yaml — pointer only, never
          copied into an app repo.
```

**What it is:** a BUILD-TIME generator. Output is plain shadcn/ui + Tailwind (MIT-compatible), so a
client app carries NO runtime dependency on the Pro account — only the build session uses it. This is
the framework's DEFAULT design-generation path (V32.11), superseding the prior "shadcn-studio = not
recommended (paid)" note now that the owner holds a Pro license.

**The 4 commands (and when the framework reaches for each):**
```
/cui  Create UI   — DEFAULT daily driver. Whole pages / multiple sections from the Pro block
                    library ("collect first, install last"). Can name blocks ("hero like Hero 2").
/iui  Inspire UI  — Pro-only. Original/creative, ONE SECTION at a time (slow/unreliable at page
                    scale). For a distinctive hero / pricing / feature section.
/rui  Refine UI   — Tweak an already-generated block. The polish pass.
/ftc  Figma→Code  — CONDITIONAL. Needs the separate Figma MCP + a Figma design. Installs matching
                    Pro blocks from Figma frames. Skip unless the design source is Figma.
```

**Phase routing (phases.md MODEL HOOKs):**
- Phase 2.8 (PA mockup) — inspiration reference only.
- Phase 3.3 (design finalization) — `/cui` (structure) → `/iui` (per-section distinctiveness) → `/rui` (polish), then compile tokens → `/design-refine` → sign off → capture DESIGN baseline.
- Phase 4 Parts 5-6 — `/cui` + `/rui` only (design FROZEN at 3.3; no `/iui`).
- Phase 7 — `/cui` new pages, `/iui` only for a genuinely new distinctive section, `/rui` polish.

**INHERIT-not-REPLACE (HARD):** generated blocks ship their own design tokens; reconcile every block
to the app's `docs/DESIGN.md` / compiled tokens (ui-rules.md Rule 12) — the block NEVER overrides the
established design system.

**Fallback:** if the Pro MCP is unreachable, fall back to the plain shadcn/ui MCP (§2.4) + the
shadcn/ui Blocks gallery — same shadcn/ui output target, lower automation.

---

## 3. Skills & Plugins

Skills are `SKILL.md` files that agents load contextually — only when the task matches the skill description. Never loaded globally (Rule 26).

### 3.1 spec-driven-core (always present)
```
Location: .github/skills/spec-driven-core/SKILL.md
Type:     Framework skill — always scaffolded
Added:    V19 (Bootstrap Step 17)
Install:  Auto-generated by Bootstrap — never install manually
```

**What it does:** Compact framework rules card. Contains the most critical V31 rules in ≤500 lines so any agent (Claude Code, Copilot, and legacy agents if re-enabled) can load it without reading the full CLAUDE.md. Enables cross-agent consistency. (Cline deprecated V31 — still reads the same format if ever re-enabled as emergency fallback.)

**When loaded:** Any agent session where the framework rules need a quick refresh.

---

### 3.2 UI UX Pro Max
```
Repo:     github.com/nextlevelbuilder/ui-ux-pro-max-skill
Type:     Design system generator skill
Added:    V12
Install:  /plugin marketplace add nextlevelbuilder/ui-ux-pro-max-skill
          /plugin install ui-ux-pro-max@ui-ux-pro-max-skill
```

**What it does:** Generates `design-system/MASTER.md` — a complete design system spec tailored to the app's industry, tone, and target users as described in PRODUCT.md. Used by Phase 2.6 to produce color palettes, typography, component patterns, and UI anti-patterns before a single line of UI is written.

**How it connects to PRODUCT.md:**
- Reads `Design Identity` section (Section K) if present
- Reads `Industry` field from App Identity
- Reads `Primary users` to calibrate density and tone
- ERP apps → dense data-first Linear/Notion aesthetic
- Government/marine apps → WCAG AA, accessible, formal

**When loaded:** Phase 2.6 only — automatically after Phase 2.5 if skill is installed.

**PRODUCT.md trigger field:**
```markdown
## Design Identity
Visual style:   [e.g. professional/enterprise, data-dense, optional dark mode]
```

---

### 3.3 Anthropic frontend-design (official)
```
URL:      https://claude.com/plugins/frontend-design
Repo:     github.com/anthropics/claude-code/plugins/frontend-design
Type:     UI generation skill — Anthropic official
Added:    V23 (Bootstrap Step 17)
Install:  /plugin install frontend-design@claude-code
          (install once per machine)
```

**What it does:** Provides bold aesthetic direction for all UI generation tasks. Typography rules, spacing systems, animation guidelines, production-quality visual standards. Works alongside UI UX Pro Max — they complement each other (UI UX Pro Max = app-specific design system; frontend-design = general production quality standards).

**When loaded:** Auto-activates on any UI generation task — Phase 4 scaffold, Phase 7 feature updates that touch the frontend.

---

### 3.4 a11y skill (accessibility)
```
Repo:     github.com/airowe/claude-a11y-skill
Type:     Accessibility enforcement skill — conditional
Added:    V23 (Bootstrap Step 17 — conditional)
Install:  /plugin install a11y-skill
          OR: npx skills add airowe/claude-a11y-skill
```

**What it does:** Enforces WCAG 2.2 AA standards (axe-core/Pa11y rulesets) before any UI component is delivered. Checks: contrast ratios (4.5:1 normal text, 3:1 large text), focus rings, alt text, ARIA labels, keyboard navigation, form labels. Pre-delivery checklist blocks merge if violations found.

**When it activates:**
- REQUIRED if PRODUCT.md Non-functional Requirements declares: `accessibility: wcag_aa`
- MGE (Marine Guardian Enterprise) — required (Philippine Data Privacy Act WCAG AA mandate)
- ERP — recommended

**PRODUCT.md trigger field:**
```markdown
## Non-functional Requirements
accessibility:  wcag_aa
```

---

### 3.5 shadcnblocks
```
Repo:     github.com/masonjames/Shadcnblocks-Skill
Type:     Component blocks catalog skill — optional
Added:    V23 (Phase 2.6 conditional)
Install:  Listed in Phase 2.6 output if shadcn/ui is the component library
          Block install: npx shadcn add @shadcnblocks/[block-name]
```

**What it does:** Registers a catalog of 2500+ pre-built shadcn/ui block compositions (hero sections, dashboards, data tables, admin layouts, marketing sections). Instead of Claude generating blocks from scratch, it selects and installs a polished pre-built block.

**When loaded:** Phase 2.6 detects if the skill is installed and adds the blocks catalog reference to MASTER.md. Phase 7 Feature Updates involving UI sections check the catalog first.

**When it's most valuable:** ERP dashboard panels, MGE map/data overlays, landing pages, admin sections.

---

### 3.6 code-simplifier
```
URL:      https://claude.com/plugins/code-simplifier
Type:     Code quality skill — Stage 2 review
Added:    V23 (Phase 7 Rule 25 Stage 2)
```

**What it does:** Adds a simplicity checklist to the Stage 2 code review: DRY (no duplication), YAGNI (no unused features), extract helpers (no functions doing more than one thing). Runs after spec-compliance check, before governance writes.

**When loaded:** Phase 7 Step 11e (Stage 2 review) — every Feature Update.

---

### 3.7 react-doctor (React diagnostics — per-phase supplementary)
```
URL:      https://github.com/millionco/react-doctor
Type:     React diagnostics linter (deterministic, oxlint-based) — supplementary, NOT a Primary Group slot
Added:    V32.4 (wired at Phase 4 Parts 5-6 / Phase 5 / Phase 7)
Install:  npx react-doctor@latest install   (its own agent installer — not a /plugin or git-clone)
```

**What it does:** Catches bad AI-generated React across state & effects, performance, architecture, security, accessibility, and bundle size. Deterministic static analysis (oxlint) — no API key, no LLM cost, reproducible. By Aiden Bai (Million.js). 11.9K★, MIT.

**When loaded:** Phase 4 Parts 5-6 (Web UI), Phase 5 (Validation), and Phase 7 (Feature Updates) — see `phases.md` Step 6 Skill Activation Plan. Not a Primary Group slot.

**How /scan-project surfaces it:** A React signal (react/next/react-native dep or `.tsx`/`.jsx` files) triggers a one-shot read-only `npx react-doctor@latest` audit → react-doctor is recommended by actual issue count → installed only on explicit approval (an **approval-gated** external install, distinct from ktx's signal-gated auto-install).

---

### 3.8 designer-skills (Design orchestration bundle — per-phase supplementary)
```
URL:      https://github.com/julianoczkowski/designer-skills
Type:     8-skill design orchestration bundle by Julian Oczkowski — supplementary, NOT a Primary Group slot
Added:    V32.5 (framework-prescribed at Phase 2.8 hand-off / Phase 4 Parts 5-6 / Phase 7)
Install:  npx skills add julianoczkowski/designer-skills   (approval-gated via /scan-project)
```

**What it does:** Encodes a deliberate design process so AI agents follow a structured path instead of producing random output. 8 slash commands: `/design-aesthetic`, `/design-tokens`, `/design-review`, `/design-refine`, `/design-system`, `/design-flow`, `/design-handoff`, `/design-audit`. Mobile-first by default, dark mode by default, 8 named aesthetic philosophies. All artifacts saved to `.design/<feature>/` — additive, never collides with `docs/PRODUCT.md` (Rule 1 still holds).

**When loaded:** Phase 2.8 → Phase 4 hand-off (consumes PA artifacts), Phase 4 Parts 5-6 (Web UI), and Phase 7 (Feature Updates) — see `phases.md` Step 6 Skill Activation Plan + the three V32.5 MODEL hooks. Not a Primary Group slot.

**V32.5 INHERIT-not-REPLACE contract:** Planning Assistant Step 7 emits `docs/DESIGN.md` (token baseline) + `docs/MOCKUP.jsx` (visual baseline) as the **human-verified design contract**. Claude Code's designer-skills MUST inherit:
- `/design-tokens` EXPANDS the DESIGN.md token table — **never regenerates**
- `/design-review` audits MOCKUP.jsx against the expanded tokens
- `/design-refine` runs ONLY on flagged components

This preserves Rule 1 (human owns PA artifacts; Claude Code cannot silently overwrite verified design intent). If PA Step 7 was skipped (no DESIGN.md), Phase 4 may invoke `/design-aesthetic` once to establish a baseline + log to PRODUCT.md Section 10 (Scenario 33 path).

**How /scan-project surfaces it:** A frontend+styling signal (React/Vue/Svelte + Tailwind/CSS-in-JS + no existing `.design/` folder + no token system + active UI files) → recommendation surfaces in Phase 4 step 3d → **approval-gated** install via `npx skills add julianoczkowski/designer-skills`. Distinct from ktx's signal-gated unattended auto-install; designer-skills + react-doctor are both approval-gated external installs.

---

### 3.9 Domain Plugin Packs (optional — install per project need)

These are installable skill packs that add domain-specific knowledge without changing the core framework.

| Pack | Install | Use when |
|---|---|---|
| `spec-driven-erp` | `/plugin install spec-driven-erp` | Powerbyte ERP — payroll tax rules, AP/AR double-entry, POS session patterns, separate-schema isolation |
| `spec-driven-govt` | `/plugin install spec-driven-govt` | MGE — DICT compliance, fisherfolk domain terms, multi-level governance (Blue Alliance → BFAR → LGU) |
| `spec-driven-payments` | `/plugin install spec-driven-payments` | Stripe webhooks, idempotency keys, PCI scope isolation, refund flows |
| `spec-driven-aws` | `/plugin install spec-driven-aws` | AWS CDK patterns, cost estimation, Serverless/EDA, Bedrock AgentCore |

**How domain packs connect to PRODUCT.md:**
- Agent reads `Industry` field → detects domain match → loads relevant pack
- ERP + `Financial data: payroll/banking` → `spec-driven-erp` triggered
- Government + marine conservation → `spec-driven-govt` triggered
- The pack's SKILL.md is read contextually — only when the current task matches its domain

### 3.10 Memory Governance Layer (V31.2)
```
Type:     Governance rules file (not a skill — an .ai_prompt/ on-demand module, V32.7)
Added:    V31.1
Location: .ai_prompt/memory-governance.md (V32.7 — was .claude/rules/memory-governance.md in V32.6.1 and earlier)
Install:  Auto-installed by /scan-project on Spec-Driven projects
          Manual: copy from specdrivenprompt/memory-governance.md
```

**What it does:** Prevents context thrashing via 5 integrated systems:
- **Section 1 — Tiered Decomposition:** 3-tier complexity classifier with scoring formula. Runs at pre-flight.
- **Section 2 — Smart Checkpoint:** Auto-persists progress to STATE.md + Claude Code memory. Zero-cost resume.
- **Section 3 — Phase Hooks:** One-liner hooks in 12 phase pre-flights enforce governance.
- **Section 4 — Architect-Execute Model:** Opus 4.6 plans, dispatches Sonnet 4.6 subagents via `Agent(model: "sonnet")`. Uses `superpowers:subagent-driven-development` for orchestration.
- **Section 5 — Mid-Project Adoption:** 4-step path + thrashing recovery for Phase 7/8 projects.
> **V32 update (2026-05-27):** Steps 2.5 (30K token budget gate) and 2.5b (Opus escalation) have been SUPERSEDED by the V32 file-size dispatch model. The Opus executor path is REMOVED. See `memory-governance.md` §1 Step 4 for the current V32 rules: `wc -l` before dispatch, ≤ 500 total lines per Sonnet task, files > 300 lines need explicit line ranges, NO Opus executor escalation.
- **THRASHING Status:** New Sonnet status detected by Opus. Response: stop immediately, re-decompose.

**When it activates:**
- Every phase pre-flight (Tiered Decomposition)
- Every task completion that modified files (Smart Checkpoint)
- ALL phase work + ad-hoc edits (Architect-Execute Model)
- Every subagent dispatch (30K Budget Gate check)

**How it connects to PRODUCT.md:**
- Opus reads PRODUCT.md sections during planning; Sonnet never reads PRODUCT.md directly
- Decomposition decisions are based on PRODUCT.md feature scope
- Smart Checkpoint captures decisions that came from PRODUCT.md interpretation

### 3.11 V32.8 — Design-as-Contract Tooling (NEW V32.8)

| Field | Value |
|---|---|
| Status | ALWAYS ACTIVE (V32.8+) |
| Added | V32.8 |
| Rules | Rule 31 (Design-as-Contract) · Rule 32 (Verifiable-Done + Learning Loop) |

**What V32.8 adds — four new tools/mechanisms:**

#### Style Dictionary v5 (DTCG Token Compiler)

| Field | Value |
|---|---|
| Package | `style-dictionary` v5 |
| Role | Compiles `docs/design-tokens.json` (W3C DTCG format) → CSS variables, Tailwind config, and Expo tokens from one source-of-truth |
| Trigger | Phase 3.3 (Planning Assistant now emits a compiled `docs/design-tokens.json`); Bootstrap Step 20 inits the pipeline |
| Command | `pnpm style-dictionary build` (reads `style-dictionary.config.json` at project root) |
| Why | Eliminates hand-authored CSS drift — every token change propagates to all platforms in one build |

Bootstrap Step 20 creates `style-dictionary.config.json` and wires the build into the Phase 5 validation pipeline.

#### Playwright Visual-Diff Gate (`toHaveScreenshot`)

| Field | Value |
|---|---|
| Package | `@playwright/test` (already in Phase 5 stack) |
| API | `expect(page).toHaveScreenshot('name.png', { maxDiffPixels: 50 })` |
| Role | Pixel-diff visual regression gate — blocks merge when the rendered output diverges from the committed baseline screenshot |
| Trigger | Phase 5 OUTPUT CONTRACT — visual-diff step runs after all 9 validation commands pass |
| Baseline management | First run: `pnpm playwright test --update-snapshots` commits baselines to `tests/snapshots/`; CI fails on any subsequent pixel diff > threshold |
| Why | Rule 31 closes the loop: token pipeline guarantees design-intent, visual gate guarantees pixel fidelity |

#### LESSONS_REGISTRY + Claude Code Stop-Hook

| Field | Value |
|---|---|
| File | `docs/LESSONS_REGISTRY.md` (per-project, agent-writable) |
| Role | Accumulates per-session findings (root causes, non-obvious gotchas, rejected approaches, cost-saving discoveries) so learnings survive context resets |
| Stop-hook | `.claude/settings.json` Stop-hook auto-appends the current session's lessons to `LESSONS_REGISTRY.md` on every Claude Code session close |
| Format | `## [date] [task-id] — [one-line finding]` + `**Root cause:**` + `**Decision:**` + `**Avoid:**` |
| Governance | `memory-governance.md §6` documents the registry schema and Stop-hook wiring |
| Why | Rule 32 requires durable learning — lessons stored only in context are lost at the next session boundary |

#### `superpowers:verification-before-completion` (Driver Skill)

| Field | Value |
|---|---|
| Skill | `superpowers:verification-before-completion` |
| Role | Drives the Rule 32 Verifiable-Done loop — enforces that every DONE claim is backed by three evidence types: (1) automated test passing, (2) human-visible screen at the correct URL, (3) log/metric confirming the feature path executed |
| Trigger | Phase 5 OUTPUT CONTRACT · Phase 7 Stage 2 review · any ad-hoc DONE declaration |
| Install | Always-available (part of the `superpowers` bundle) — no separate install needed |
| Why | DONE without evidence is a claim, not a fact; this skill structures the verification ritual into a repeatable checklist |

**How the four tools connect:**

```
docs/design-tokens.json (DTCG source)
  └─ style-dictionary build → CSS vars + Tailwind + Expo tokens   ← Rule 31 compile
        └─ Phase 5 Playwright toHaveScreenshot gate                 ← Rule 31 pixel gate
              └─ superpowers:verification-before-completion         ← Rule 32 evidence check
                    └─ LESSONS_REGISTRY + Stop-hook                 ← Rule 32 learning loop
```

**Change history:**
- V32.8: Added. All four mechanisms are new. Style Dictionary v5 + Playwright visual gate enforce Rule 31; LESSONS_REGISTRY + Stop-hook + verification-before-completion enforce Rule 32.

---

### 3.12 V32.9 — Compliance + Data Privacy Skills (NEW V32.9)

Two new skills prescribed by **Rule 33** (`privacy.md` deliverable #23). Both are registered in `src/data/skills.js` and surfaced by `/scan-project`.

#### ph-data-privacy

| Field | Value |
|---|---|
| Skill ID | `ph-data-privacy` |
| Repo | `bonitobonita24/ph-data-privacy` |
| Category | Security |
| Author | Powerbyte IT Solutions (Internal) |
| Status | On-demand — triggered by Rule 33 |

**What it does:** Philippine Data Privacy Act (RA 10173 / NPC) compliance guide for Claude Code. Covers NPC registration, lawful bases, data subject rights (access/correction/erasure/portability/object), Privacy Impact Assessment (PIA), breach notification (72h NPC + affected data subjects), data sharing agreements, retention schedules, DPO designation, and consent form templates. Also covers WCAG 2.2 AA requirements from DICT MC 004-2020.

**When it activates:**
- Phase 4 Part 3 (schema) + Part 4 (auth/RBAC) — when PRODUCT.md §12 Compliance section populated
- Phase 7 Feature Updates touching user data, consent, or audit trails
- Any phase when PRODUCT.md Industry = Government / LGU
- Triggered explicitly via Rule 33: `Read .ai_prompt/privacy.md`

**How /scan-project surfaces it:** Compliance signal (gov/LGU industry, personal data entities in schema, NPC/DICT mention in PRODUCT.md) → `ph-data-privacy` recommended as approval-gated install.

#### accessibility-agents

| Field | Value |
|---|---|
| Skill ID | `accessibility-agents` |
| Repo | `Community-Access/accessibility-agents` |
| Category | Testing |
| Author | Community |
| Status | Framework-prescribed at Phase 4 Parts 5-6 + Phase 5 |

**What it does:** Multi-agent accessibility testing suite. Runs automated WCAG 2.2 AA checks via axe-core, Pa11y, and Playwright assistive-technology simulation (screen reader, keyboard-only nav, forced-colors). Produces a per-route scorecard with ARIA role tree, contrast ratios, focus-order map, and interactive element labels.

**When it activates:**
- Phase 4 Parts 5-6 (UI wiring) — WCAG audit pass after components are wired
- Phase 5 OUTPUT CONTRACT — accessibility gate runs after the 9 validation commands
- **Gov/LGU apps:** WCAG 2.2 AA is a HARD GATE (DICT MC 004) — Phase 5 cannot close until `accessibility-agents` returns green. This is enforced by ui-rules.md R13 (V32.9).

**How /scan-project surfaces it:** Frontend signal (React/Next.js + UI files) → `accessibility-agents` recommended. Gov/LGU signal → promoted to mandatory gate.

**Change history:**
- V32.9: Both skills added. `ph-data-privacy` by Powerbyte (Internal); `accessibility-agents` from Community-Access. Both wired into `src/data/skills.js` + CLAUDE.md phase→skill table per Surface Additions Policy.

---

## 4. Infrastructure Tools

These are not AI skills — they are system services generated and managed by the framework.

### 4.1 pgAdmin 4
```
Type:     Database management UI — always scaffolded
Added:    V16
Phase:    Phase 4 Part 7 generates compose file + pgadmin-servers.json
```

Auto-generated on all environments (dev/staging/prod). Connects to PostgreSQL on first launch via pre-configured `pgadmin-servers.json`. Credentials auto-generated by Phase 3, stored in `CREDENTIALS.md`.

**Access:** `http://localhost:${PGADMIN_PORT}` (check `.env.dev` for exact port — random per Rule 22)

---

### 4.2 Komodo (deployment manager)
```
Docs:     https://komo.do/docs/intro
Type:     Self-hosted deployment UI — optional
Added:    V23 (Scenario 32)
Phase:    Phase 3 → CREDENTIALS.md. Scenario 32 = full setup guide.
```

Manages staging/prod deployments without SSH. V27 model: GitHub Actions builds the Docker image → pushes to Docker Hub (:staging-latest + :latest + :sha) → Komodo staging auto-update polls for new :staging-latest and auto-redeploys → human verifies staging → clicks Deploy in Komodo UI for production. No webhooks needed for recommended path. Webhook path preserved as optional legacy option.

**PRODUCT.md signal:** If `Infrastructure Notes` declares `Komodo: enabled`, Phase 3 generates the `KOMODO_*` credentials block in `CREDENTIALS.md` and Bootstrap Step 18 collects the Komodo UI URL (webhook URLs are optional in V27).

---

### 4.4 Xendit (payment gateway — framework default)

```
Type:     External SaaS — conditional (only if app accepts payments)
Added:    V27
Phase:    Phase 2 Section G2 → PRODUCT.md. Bootstrap Step 18 Section 4.5 → CREDENTIALS.md.
```

Framework default payment gateway for all SEA markets (ID, PH, MY, TH, VN). Accepts cards, e-wallets, bank transfers, OTC/over-the-counter, and QR payments. Webhook verification via `x-callback-token` header (constant-time comparison). API uses Basic Auth with secret key as username.

**PRODUCT.md signal:** If any module or workflow mentions payment acceptance, Phase 2 Section G2 asks payment questions and locks `payment.gateway: xendit` in DECISIONS_LOG.md.

**Credentials:** Bootstrap Step 18 Section 4.5 collects 5 values: test secret key, live secret key, test public key, live public key, webhook verification token. Stored in dedicated CREDENTIALS.md Xendit section. Written to `.env.dev` (TEST keys), `.env.staging/.env.prod` (LIVE keys). All gitignored.

**Security:** Secure Code Generation section enforces 6 rules: x-callback-token verification, idempotent webhook handlers, payload validation, endpoint security, key isolation, queue processing recommendation.

**Docs:** https://docs.xendit.co/apidocs · **Webhooks:** https://docs.xendit.co/docs/handling-webhooks · **Dashboard:** https://dashboard.xendit.co

---

### 4.5 Cloudflare Turnstile (bot protection — framework default)

```
Type:     External SaaS — framework default (enabled unless opted out)
Added:    V27
Phase:    Phase 2 Section H → PRODUCT.md. Bootstrap Step 18 Section 4.6 → CREDENTIALS.md.
```

Framework default bot protection replacing CAPTCHA. Invisible or managed challenges — visitors rarely see anything. WCAG 2.2 AAA compliant. FREE tier: 20 widgets (1 per app sufficient), 10 hostnames per widget (only prod domain needed — dev + staging use test keys = 1 hostname per app).

**Protected pages by default:** login, registration, password reset, contact forms, payment pages (if Xendit enabled). Authenticated pages NOT protected (rate limiting suffices).

**Credentials:** Bootstrap Step 18 Section 4.6 collects Site Key (public, client-safe) + Secret Key (server-only). Dev AND staging use Cloudflare's official test keys (always-pass) — zero real widget needed except in prod. Stored in CREDENTIALS.md Turnstile section + .env files. All gitignored.

**Integration:** Client-side `@marsidev/react-turnstile` React component (explicit rendering). Server-side `siteverify` POST validation mandatory — token is single-use, expires in 300s. CSP must allow `challenges.cloudflare.com`.

**Multi-tenant SaaS:** Each unique hostname uses 1 of 10 slots. Subdomains of your domain can use wildcard. Custom tenant domains need individual hostname entries. Phase 2 Section H asks about hostname count.

**Docs:** https://developers.cloudflare.com/turnstile/ · **Testing:** https://developers.cloudflare.com/turnstile/troubleshooting/testing/

---

### 4.3 Docker Compose Services (always generated)

These are scaffolded by Phase 4 Parts 6–7 for every app regardless of PRODUCT.md:

| Service | Container | Dev Port |
|---|---|---|
| PostgreSQL + PgBouncer | `${COMPOSE_PROJECT_NAME}_postgres` | random base (Rule 22) |
| Valkey (Redis fork) | `${COMPOSE_PROJECT_NAME}_valkey` | base+2 |
| MinIO (S3-compatible) | `${COMPOSE_PROJECT_NAME}_minio` | base+3 (API), base+4 (Console) |
| MailHog (dev SMTP) | `${COMPOSE_PROJECT_NAME}_mailhog` | base+5 (SMTP), base+6 (UI) |
| pgAdmin 4 | `${COMPOSE_PROJECT_NAME}_pgadmin` | base+7 |
| Next.js app | `${COMPOSE_PROJECT_NAME}_app` | base+10 |
| BullMQ worker | `${COMPOSE_PROJECT_NAME}_worker` | base+11 (if jobs declared) |

**All ports are unique per project** — generated once in Phase 3 using a random base in range 40000–49999. Never conflicts with other projects or standard services.

---

## 5. Model Routing

Locked in `inputs.yml` during Phase 3. Every project gets the same default routing unless explicitly changed in PRODUCT.md or DECISIONS_LOG.md.

```yaml
models:
  planning:   claude-code           # Phase 2 interview — CLAUDE.md auto-loaded
  execution:  claude-sonnet-4-6     # Phase 4-8, Feature Updates — V31 primary via Claude Code
  governance: gemini-2.5-flash-lite # Changelog/doc writes — cheapest
  escalation: claude-opus-4-6       # Complex multi-file bugs — when Sonnet 4.6 pivots
```

### Why this routing

| Model | Role | Reason |
|---|---|---|
| **Claude Code** | Planning | Auto-loads CLAUDE.md. Best for spec interview, ambiguity resolution, Phase 2 reasoning |
| **Claude Sonnet 4.6** | Execution (V31 primary) | Promoted to primary in V30. Handles ALL Phase 4-8 scaffold and Feature Updates via Claude Code. Replaces MiniMax M2.5 which was used via Cline (Cline deprecated V31). |
| **Gemini 2.5 Flash-Lite** | Governance | Cheapest capable model. Governance writes (CHANGELOG_AI, DECISIONS_LOG, IMPLEMENTATION_MAP) are low-complexity document updates |
| **Claude Opus 4.6** | Escalation | Invoked only when Sonnet 4.6 hits 2-strike failure on complex multi-file reasoning. Not the default — escalation only |

### Legacy MiniMax M2.5 routing (historical — V19 through V30)

Before V31 Cline deprecation, MiniMax M2.5 was the default execution model on OpenRouter free tier because Cline ran execution via OpenRouter. V31 in-place deprecation moved execution to Claude Code + Claude Sonnet 4.6 directly. MiniMax references below are preserved as historical context only:

- V19-V30 default execution: `minimax/minimax-m2.5` on OpenRouter free tier
- V19-V30 OpenRouter troubleshooting: if `404 No endpoints available`, relax privacy settings at https://openrouter.ai/settings/privacy or fallback to `deepseek/deepseek-v3`
- V19-V30 execution fallback: `deepseek/deepseek-v3` (comparable SWE-Bench score, free tier, strong at spec decomposition)
- V31+: execution routes through Claude Code; MiniMax/DeepSeek only apply if user manually re-enables Cline as emergency fallback (not part of framework routing)

---

## 6. How Tools Connect to PRODUCT.md

This is the core of intelligent app building — every tool decision traces back to what's declared in PRODUCT.md.

### 6.1 The full decision chain

```
PRODUCT.md field          → Tool/skill activated
─────────────────────────────────────────────────────────────────────
Industry: ERP             → spec-driven-erp plugin loaded contextually
Industry: Government/     → spec-driven-govt plugin loaded
  Marine Conservation
Financial data: payroll/  → separate PostgreSQL schema per tenant
  banking                   (not shared schema — override of default)
Mobile App declared       → Expo + WatermelonDB scaffold in Phase 4
  offline-first: yes        Mobile: React Native Reusables + NativeWind
  platform: iOS/Android     Push notifications: Expo Push wired
accessibility: wcag_aa    → a11y skill activated (blocking pre-delivery)
Design Identity present   → UI UX Pro Max runs in Phase 2.6
  (Section K)               Generates design-system/MASTER.md
Background jobs declared  → BullMQ + Valkey worker scaffolded
  (BullMQ: yes)             docker-compose.cache.yml + worker service
File uploads declared     → MinIO scaffold + S3 env vars
  (storage: minio)          presigned URL helper generated
Realtime declared         → SSE endpoint scaffolded (web)
  (realtime: sse)           tRPC + React Query polling (mobile)
Tenancy: multi            → All 6 security layers active (L1–L6)
  (shared schema + RLS)     RLS policies enabled in Prisma schema
Tenancy: single           → L3, L5, L6 active; L1, L2, L4 dormant
                              Zero-migration upgrade path scaffolded
docker.publish: true      → GitHub Actions docker-publish.yml
                              push.sh (dev→staging→prod)
                              COMMANDS.md docker push section
WCAG AA + government      → a11y skill required (not optional)
  domain                    Pre-delivery checklist blocks merge
```

### 6.2 Phase-by-phase tool activation

```
Phase 0  Bootstrap
  → Writes .vscode/mcp.json (SocratiCode + Context7)
  → Bootstrap Step 17: spec-driven-core skill + prompts for frontend-design, a11y
  → Bootstrap Step 18: CREDENTIALS.md written (GitHub, Docker Hub, SMTP, Komodo, Xendit, Turnstile)

Phase 2  Discovery Interview (Claude Code)
  → CREDENTIALS.md pre-flight gate
  → Reads PRODUCT.md: locks tenancy, entities, security, infrastructure
  → Locks model routing in inputs.yml

Phase 2.6  Design System (Claude Code, conditional — Cline deprecated)
  → ONLY if UI UX Pro Max skill installed AND Design Identity in PRODUCT.md
  → Reads Industry + Design Identity → generates design-system/MASTER.md
  → Embeds Vercel Web Interface Guidelines in MASTER.md
  → Detects a11y requirement from PRODUCT.md → flags for enforcement
  → Detects shadcnblocks skill → adds block catalog reference to MASTER.md
  → Detects frontend-design plugin → notes it for Phase 4/7 UI tasks

Phase 2.7  Spec Stress-Test (Claude Code, auto)
  → Pressure-tests PRODUCT.md for gaps before any code is written
  → Blocks Phase 3 if required sections are missing or contradictory

Phase 3  Generate Spec Files (Claude Code)
  → inputs.yml: tech stack, ports (random), model routing, git strategy
  → CREDENTIALS.md validation: GitHub, Docker Hub, SMTP all present
  → Random port base generated (40000–49999) → all service ports derived

Phase 4  Full Scaffold (Claude Code, Part-by-Part — Cline deprecated)
  → Part 6: Docker Compose files for all services
  → Part 7: pgAdmin compose + pgadmin-servers.json + push.sh + COMMANDS.md
  → Part 8: SocratiCode initial index built
  → Phase 4 reads design-system/MASTER.md before generating any UI component
  → Phase 4 reads PRODUCT.md entities section → Prisma schema
  → Separate schema generated if PRODUCT.md declares payroll/banking sensitivity

Phase 5  Validation (Claude Code — Cline deprecated)
  → pnpm audit CI gate (Rule 18 security)
  → All 9 validation commands must pass

Phase 6  Docker Services (Claude Code — Cline deprecated)
  → WSL2 terminal — docker compose commands
  → All services up, db:migrate + db:seed

Phase 7  Feature Update Loop (Claude Code — the daily loop · Cline deprecated)
  → Step 2: SocratiCode codebase_search (semantic discovery)
  → Step 3: code-review-graph get_impact_radius_tool (blast radius)
  → Step 9: Context7 "use context7" appended for any library task
  → Stage 2 review: code-simplifier checklist (DRY, YAGNI, helpers)
  → a11y checklist runs if skill installed + wcag_aa declared
```

### 6.3 How app intelligence is built

The framework doesn't guess what to build. Every structural decision traces to PRODUCT.md:

1. **Entities** declared in PRODUCT.md → Prisma schema with correct relations, indexes, tenant isolation
2. **Roles + Permissions** → tRPC RBAC middleware (L3) + AuditLog entries per mutation
3. **Data Sensitivity** → determines DB isolation strategy (shared vs separate schema)
4. **Mobile declared** → Expo project scaffolded in `apps/mobile/`, WatermelonDB for offline entities, Expo Push wired
5. **Integrations listed** → env vars pre-declared in .env.example, service wrapper scaffolded
6. **Out of Scope** → agents never scaffold these — prevents hallucinated features

---

## 7. Complete Install Checklist

### One-time (per machine)

```bash
# 1. VS Code extensions
# Install via Extensions panel:
#   - Claude Code (Anthropic)
#   - Cline  (⚠ deprecated V31 — optional emergency fallback only; not required)
#   - GitHub Copilot
#   - SpecStory
#   - Remote-WSL (for WSL2 native dev)

# 2. code-review-graph MCP (Claude Code plugin)
claude plugin add tirth8205/code-review-graph
# Requires: Python 3.10+, uv package manager
# uv install: curl -LsSf https://astral.sh/uv/install.sh | sh

# 3. Node + pnpm (WSL2 Ubuntu terminal)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
nvm install 22 && nvm use 22 && npm install -g pnpm
```

### Per-project (after Bootstrap)

```bash
# 4. UI UX Pro Max (if design system needed)
/plugin marketplace add nextlevelbuilder/ui-ux-pro-max-skill
/plugin install ui-ux-pro-max@ui-ux-pro-max-skill

# 5. Anthropic frontend-design plugin (recommended for all UI work)
/plugin install frontend-design@claude-code

# 6. a11y skill (required if WCAG AA declared in PRODUCT.md)
/plugin install a11y-skill
# OR: npx skills add airowe/claude-a11y-skill

# 7. Domain plugin packs (install what matches the app)
# For Powerbyte ERP:
/plugin install spec-driven-erp

# For Marine Guardian Enterprise:
/plugin install spec-driven-govt

# 8. After Phase 4 completes — build code-review-graph index
code-review-graph build

# 9. SocratiCode — automatic via .vscode/mcp.json
# (Docker Desktop must be running)
# On first use: auto-pulls Qdrant + Ollama Docker images (~5 min one-time)
```

### Environment verification

```bash
# Verify all MCPs are wired
cat .vscode/mcp.json   # should have socraticode + context7 entries

# Verify skills are present
ls .github/skills/     # should have spec-driven-core/ at minimum

# Verify Docker services are running
docker ps | grep ${COMPOSE_PROJECT_NAME}

# Verify SocratiCode index
# In Claude Code: ask "codebase_status" — should return READY
```

---

## 8. Monorepo Package Structure

Every project scaffolded by Phase 4 uses this exact package layout. Agents derive it entirely from `inputs.yml` — nothing is hardcoded.

```
[project-root]/
├── apps/
│   ├── [web]/              Next.js App Router — main web app
│   ├── [mobile]/           Expo managed workflow — iOS + Android (conditional)
│   └── [admin]/            Next.js — admin panel (conditional)
├── packages/
│   ├── shared/             Zod schemas + TypeScript types for ALL entities
│   ├── api-client/         Typed tRPC client — used by apps, NEVER by packages/db
│   ├── db/                 Prisma schema + migrations + AuditLog + tenant guard
│   ├── ui/                 shadcn/ui + Tailwind (web) + React Native Reusables (mobile)
│   ├── jobs/               BullMQ queue definitions + workers (conditional: jobs.enabled)
│   └── storage/            MinIO/S3 typed wrapper (conditional: storage.enabled)
├── tools/                  Governance tools — .mjs files (only .js exception)
│   ├── validate-inputs.mjs     Validates inputs.yml against inputs.schema.json
│   ├── check-env.mjs           Validates all required env vars exist and are non-empty
│   ├── hydration-lint.mjs      Detects React/Next.js hydration anti-patterns
│   └── check-product-sync.mjs  Bidirectional Rule 9 guard — exits 1 if spec drifts
├── deploy/
│   ├── compose/dev|stage|prod/ Separate compose files per service group
│   └── compose/start.sh        One-command startup: bash start.sh dev up -d
├── .github/
│   ├── workflows/ci.yml        GitHub Actions — governance gates + Turbo matrix
│   ├── workflows/docker-publish.yml  Docker Hub image push on merge to main
│   └── skills/                 Agent skills directory (spec-driven-core + plugins)
├── design-system/
│   ├── MASTER.md               Generated by Phase 2.6 — read before any UI task
│   └── pages/                  Per-page design overrides
├── docs/                       9 governance documents (agent-read on every task)
├── .cline/                     Cline memory + task files + handoffs
├── .specstory/                 SpecStory passive capture (auto-appended, never delete)
├── CLAUDE.md                   Master prompt — auto-loaded every session
├── .clinerules                 Cline execution rules
├── inputs.yml                  Machine-readable app spec (agent-owned)
├── inputs.schema.json          JSON Schema for inputs.yml validation
├── CREDENTIALS.md              All secrets — gitignored, never read into context
└── COMMANDS.md                 Master command reference — all docker, db, test, git cmds
```

### Key ownership rules
- `PRODUCT.md`, `CLAUDE.md`, `.clinerules`, `.vscode/mcp.json` → **Human-owned**
- Everything else → **Agent-owned** (never edit manually — use Phase 7)
- `CREDENTIALS.md` → **Agent/Gitignored** — agents write it, never read it back into context

---

## 9. Governance Tools (tools/ directory)

Four `.mjs` files generated by Phase 4 Part 7. These are the only `.mjs` files in the project (the one exception to Rule 12 TypeScript-everywhere — they must be Node scripts for CI compatibility).

### 9.1 validate-inputs.mjs
Validates `inputs.yml` against `inputs.schema.json`. Runs in CI (`pnpm tools:validate-inputs`) and Phase 5. Exits 1 if any required field is missing or wrong type. Prevents agents from building on a broken spec.

### 9.2 check-env.mjs
Validates that every env var declared in `inputs.yml` exists in the current `.env.*` file and is non-empty. Runs at Phase 5 and CI. Catches missing secrets before deployment. No more "undefined DATABASE_URL" runtime crashes.

### 9.3 hydration-lint.mjs
**This is the tool that prevents hydration errors.** Scans the Next.js `apps/[web]/src/` directory for known hydration anti-patterns:

| Pattern detected | Why it causes hydration errors |
|---|---|
| `typeof window !== 'undefined'` in render | Server/client HTML mismatch |
| `Math.random()` or `Date.now()` in render | Non-deterministic render output |
| `localStorage` / `sessionStorage` in render | Not available server-side |
| `new Date()` without timezone normalization | Server UTC vs client local time |
| Dynamic class names without `cn()` | Class order differs server/client |
| `useLayoutEffect` without SSR guard | Runs on server, throws warning |
| Missing `suppressHydrationWarning` on time elements | Known-safe dynamic timestamps |

Runs as part of Phase 5 validation and CI. Exits 1 on any violation. Agents must fix before merge.

### 9.4 check-product-sync.mjs
Implements Rule 9 (bidirectional governance). Checks:
- Direction A: Every entity in `PRODUCT.md` has a corresponding Prisma model
- Direction B: Every Prisma model is declared in `PRODUCT.md`
- Every module declared in `PRODUCT.md` has a corresponding tRPC router
- `<private>` tags in `PRODUCT.md` have not leaked into governance docs

Exits 1 on any violation. Runs in CI and Phase 5. This is the primary guard against spec drift.

---

## 10. Security Stack (always scaffolded — V18+)

These four security primitives are generated in every app regardless of PRODUCT.md. They are not optional.

### 10.1 HTTP Security Headers
Generated in `next.config.ts` for every Next.js app:

```ts
const securityHeaders = [
  { key: 'X-DNS-Prefetch-Control', value: 'on' },
  { key: 'Strict-Transport-Security', value: 'max-age=63072000; includeSubDomains; preload' },
  { key: 'X-Frame-Options', value: 'SAMEORIGIN' },
  { key: 'X-Content-Type-Options', value: 'nosniff' },
  { key: 'Referrer-Policy', value: 'origin-when-cross-origin' },
  { key: 'Content-Security-Policy', value: "default-src 'self'; ..." },
];
```

### 10.2 LRU Rate Limiter
Generated in `src/server/lib/rate-limit.ts`. Four pre-configured tiers:

| Tier | Limit | Use |
|---|---|---|
| `public` | 30 req/min | Unauthenticated endpoints |
| `auth` | 10 req/min | Login, register, password reset |
| `api` | 120 req/min | Authenticated API calls |
| `upload` | 20 req/min | File upload endpoints |

In-memory store (LRUCache) for single-instance. Swap to Redis (1 line) for multi-instance prod.

### 10.3 DOMPurify Sanitizer
Generated in `src/server/lib/sanitize.ts`. Used anywhere user-submitted content is stored and rendered as HTML. Prevents XSS from stored content.

### 10.4 pnpm audit CI gate
`pnpm audit --audit-level=high` runs as Phase 5 step 9. HIGH or CRITICAL CVEs block merge. CRITICAL CVEs always block — no documented exceptions. HIGH CVEs with no available fix are documented in `DECISIONS_LOG.md` with explicit risk acceptance.

---

## 11. Mobile Stack Details (Expo — conditional)

Scaffolded only when `mobile` is declared in PRODUCT.md. Internal ERP distribution (APK/IPA direct install) vs App Store differs only in `eas.json` — same code either way.

### 11.1 Core mobile packages
| Package | Purpose |
|---|---|
| `expo` (managed workflow) | Cross-platform build system |
| `expo-router` | File-based navigation (same pattern as Next.js App Router) |
| `react-native-reusables` | shadcn/ui equivalent for React Native |
| `nativewind` | Tailwind CSS for React Native — same class names as web |
| `watermelondb` | SQLite-backed offline database with sync protocol |
| `expo-location` | GPS access — foreground and background tracking |
| `expo-camera` + `expo-image-manipulator` | Photo capture + compression |
| `expo-notifications` | Push notification token management |
| `react-native-maps` | Map rendering (GPS patrol/DTR location display) |

### 11.2 Offline-first sync pattern
When `offline-first: yes` in PRODUCT.md:
- WatermelonDB stores local copies of declared offline entities
- `src/sync/` contains sync queue + conflict resolution logic
- Server version wins on conflict (configurable per entity in PRODUCT.md)
- Auto-syncs on reconnect — no manual trigger needed
- Queued mutations survive app restart

### 11.3 Push notification lifecycle
```
App login → request permission → Expo SDK returns token
→ server upserts PushToken record for user
→ BullMQ job dispatches push via Expo Push API
→ FCM (Android) / APNs (iOS) delivers to device
```

### 11.4 PRODUCT.md mobile trigger fields
```markdown
## Mobile App
Platform:      iOS + Android
Framework:     Expo managed workflow
Distribution:  internal/enterprise [or] App Store + Play Store
Offline-first: yes
  [list which entities work offline]
Push notifications: yes
  [list notification events]
Native features:
  GPS: [purpose]
  Camera: [purpose]
  Biometrics: [yes/no — for app unlock only, not auth]
Deep linking: yes
  [list notification → screen mappings]
```

---

## 12. Map Libraries

### Web: Leaflet.js + OpenStreetMap
Used in ERP for DTR GPS clock-in/out location display. Chosen over Google Maps:
- Free, MIT licensed
- Zero API key required
- Zero ongoing cost
- Sufficient for pin/marker display on a map
- Can migrate to Google Maps or Mapbox if advanced geo features needed later

Decision locked: `DECISIONS_LOG.md` — "Maps: Leaflet.js + OpenStreetMap — chosen over Google Maps API to eliminate API costs"

### Mobile: react-native-maps
Used in BlueSentinel (MGE mobile) for patrol route tracking. GeoJSON route stored in `Patrol.routeGeoJson` (JSONB field in PostgreSQL). 30-second GPS interval via Expo Location background task.

---

## 13. TypeScript Strictness Config

Every project uses this exact `tsconfig.base.json`. All flags are required. None are optional.

```json
{
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  }
}
```

ESLint rules (always enforced):
- `@typescript-eslint/no-explicit-any: error` — no `any` types ever
- `@typescript-eslint/no-unsafe-assignment: error` — no implicit `any` from untyped
- `@typescript-eslint/strict-boolean-expressions: error` — no truthy coercion

**These strictness flags are why hydration-lint exists.** `noUncheckedIndexedAccess` and `exactOptionalPropertyTypes` catch entire classes of runtime errors at compile time — but they also surface edge cases that cause hydration mismatches. The lint tool catches what TypeScript can't see.

---

## 14. CI/CD Pipeline

### GitHub Actions (always generated — Phase 4 Part 8)

```yaml
# Two jobs — governance gates must pass before quality matrix runs
jobs:
  governance:          # validate-inputs, check-env, check-product-sync
  quality:
    matrix:
      task: [lint, typecheck, test, build]  # 4 parallel jobs via Turbo
```

The `concurrency` block cancels in-progress runs on new push — saves CI minutes.

### Docker Hub pipeline (conditional: docker.publish: true)

```
Push to main → GitHub Actions docker-publish.yml
→ Builds Docker image → pushes :latest + :staging-latest + :sha to Docker Hub
→ Komodo staging auto-update detects new :staging-latest → auto-redeploys staging
→ Human verifies staging → clicks Deploy in Komodo UI → prod pulls :latest from Docker Hub
GitHub Actions never contacts Komodo directly. Docker Hub is the handoff point (V27).
```

Manual promotion path: `bash deploy/compose/push.sh dev|staging|prod`

All commands documented in `COMMANDS.md` at project root.

---

## 15. Secure Code Generation (V25+)

The Master Prompt contains a SECURE CODE GENERATION section (between GLOBAL RULES and FILE DELIVERY RULES) that enforces security constraints on every line of generated code. This is not a tool or skill — it is a mandatory instruction block that Claude Sonnet 4.6 via Claude Code (V31 primary) and all other agents must follow during Phase 4 scaffold and Phase 7 Feature Updates.

**16 sub-sections, 15+ threat scenarios addressed.** Key constraints:
- L6 Prisma extension uses `$allOperations` — covers delete, count, aggregate (previously unguarded)
- Route Handlers and Server Actions prohibited for app logic (bypass tRPC)
- Tenant middleware must cross-check URL slug against session
- Superadmin operations use dedicated `platformPrisma` client without L6 extension
- File downloads verify tenantId matches storage path prefix
- Cron jobs iterate over tenants explicitly — no unscoped queries
- Auth errors never reveal whether accounts or tenants exist
- CSRF protection documented (tRPC + SameSite inherently resistant)
- SSRF prevention (private IP range rejection, URL parsing, DNS rebinding)
- Session invalidation on role/tenant change
- Tiered global rate limiting (auth/API/public)

**Companion file:** `Post_Generation_Security_Checklist_v31.md` — 84 verification items for human or cross-AI audit after code generation.

**This section is read by agents via CLAUDE.md.** Agents do not need to load it separately.

---

## 16. UI Component Ecosystem (NEW V29)

The Master Prompt contains a UI COMPONENT RULES section that enforces shadcn/ui as the ONLY component library for all generated UI. This is not optional — agents follow these 10 rules during Phase 4 scaffold and Phase 7 Feature Updates.

**Locked component stack (all MIT, all free, all open source):**

| Layer | Library | Purpose | Install Pattern |
|---|---|---|---|
| Core components | shadcn/ui | 57+ Radix-based primitives | `npx shadcn@latest add [component]` |
| Charts | Recharts (via shadcn/ui Chart) | Dashboard charts, area/bar/line/pie | `npx shadcn@latest add chart` |
| Data tables | TanStack Table (via shadcn/ui Data Table) | Sortable, filterable, paginated tables | `npx shadcn@latest add data-table` |
| Forms | React Hook Form + Zod (via shadcn/ui Form) | Client-side validation matching server Zod schemas | `npx shadcn@latest add form` |
| Icons | lucide-react | Icon set (shadcn/ui dependency) | Already installed with shadcn/ui |
| Maps (default) | Leaflet.js + OpenStreetMap | Simple pins/markers, zero API cost | `npm install leaflet react-leaflet` |
| Maps (advanced) | mapcn (MapLibre GL) | Routes, layers, vector tiles, auto-theming | `npx shadcn@latest add https://mapcn.dev/maps/map.json` |
| Complex UI | Kibo UI | Kanban, Gantt, Editor, Dropzone, Code Block | `npx kibo-ui add [component]` |
| Component discovery | shadcn MCP server | Natural language search + install | Auto-installed in `.vscode/mcp.json` |

**NEVER import:** MUI, Ant Design, Chakra UI, Mantine, DaisyUI, Chart.js (use Recharts), D3 (standard charts), heroicons, react-icons, font-awesome.

**Phase 4 Part 5** runs `npx shadcn@latest init` and installs base components before generating any UI code. Additional components added conditionally based on PRODUCT.md declarations.

**Community discovery resources (optional, not framework defaults):**
- awesome-shadcn-ui: https://github.com/birobirobiro/awesome-shadcn-ui (200+ community registries)
- shadcnregistry.com: https://shadcnregistry.com (searchable index)

**Change history:**
- V29: Initial — UI COMPONENT RULES section, shadcn MCP in Bootstrap Step 10, Phase 4 Part 5 shadcn init, Kibo UI + mapcn as community registries

---

## Appendix — All Referenced Links

### AI Agents & Tools
| Tool | Link |
|---|---|
| SocratiCode | `npx socraticode` (auto via mcp.json) |
| code-review-graph | https://github.com/tirth8205/code-review-graph |
| Context7 | https://claude.com/plugins/context7 |
| Anthropic frontend-design | https://claude.com/plugins/frontend-design |
| Anthropic frontend-design SKILL.md | https://github.com/anthropics/claude-code/blob/main/plugins/frontend-design/skills/frontend-design/SKILL.md |
| UI UX Pro Max | https://github.com/nextlevelbuilder/ui-ux-pro-max-skill |
| a11y skill | https://github.com/airowe/claude-a11y-skill |
| Shadcnblocks skill | https://github.com/masonjames/Shadcnblocks-Skill |
| code-simplifier | https://claude.com/plugins/code-simplifier |
| SpecStory | VS Code Extensions marketplace — search "SpecStory" |
| awesome-claude-skills (discovery) | https://github.com/travisvn/awesome-claude-skills |
| jezweb skills (discovery) | https://github.com/jezweb/claude-skills |
| agentskill.sh (skill discovery) | https://agentskill.sh |

### Deployment & Infrastructure
| Tool | Link |
|---|---|
| Komodo | https://komo.do/docs/intro |
| Xendit API | https://docs.xendit.co/apidocs |
| Xendit Webhooks | https://docs.xendit.co/docs/handling-webhooks |
| Xendit Dashboard | https://dashboard.xendit.co |
| Cloudflare Turnstile | https://developers.cloudflare.com/turnstile/ |
| Turnstile Testing Keys | https://developers.cloudflare.com/turnstile/troubleshooting/testing/ |
| OpenRouter (model gateway) | https://openrouter.ai |
| OpenRouter privacy settings | https://openrouter.ai/settings/privacy |

### UI & Design
| Resource | Link |
|---|---|
| shadcn/ui (home) | https://ui.shadcn.com |
| shadcn/ui components | https://ui.shadcn.com/docs/components |
| shadcn/ui theming | https://ui.shadcn.com/docs/theming |
| shadcn/ui dark mode | https://ui.shadcn.com/docs/dark-mode |
| shadcn/ui charts | https://ui.shadcn.com/docs/components/radix/chart |
| shadcn/ui chart examples | https://ui.shadcn.com/charts/area |
| shadcn/ui blocks | https://ui.shadcn.com/blocks |
| shadcn/ui forms | https://ui.shadcn.com/docs/forms |
| shadcn/ui MCP server | https://ui.shadcn.com/docs/mcp |
| shadcn/ui skills (agent rules) | https://ui.shadcn.com/docs/skills |
| shadcn/ui registry directory | https://ui.shadcn.com/docs/directory |
| shadcn/ui registry (build your own) | https://ui.shadcn.com/docs/registry |
| shadcn/ui monorepo setup | https://ui.shadcn.com/docs/monorepo |
| shadcn/ui CLI | https://ui.shadcn.com/docs/cli |
| shadcn/ui llms.txt | https://ui.shadcn.com/llms.txt |
| Kibo UI (complex components — MIT) | https://www.kibo-ui.com |
| Kibo UI components | https://www.kibo-ui.com/components |
| mapcn (shadcn-native maps — MIT) | https://www.mapcn.dev |
| mapcn docs | https://www.mapcn.dev/docs |
| mapcn GitHub | https://github.com/AnmolSaini16/mapcn |
| awesome-shadcn-ui (discovery list) | https://github.com/birobirobiro/awesome-shadcn-ui |
| shadcnregistry.com (searchable index) | https://shadcnregistry.com |
| Vercel Web Interface Guidelines | https://vercel.com/design/guidelines |
| Vercel guidelines (agent skill) | https://vercel.com/changelog/web-interface-guidelines-now-available-as-an-agent-command |
| Vercel agent resources | https://vercel.com/docs/agent-resources/skills |
| React Native Reusables | https://rnr-docs.vercel.app |
| NativeWind | https://www.nativewind.dev |

### Mobile
| Resource | Link |
|---|---|
| Expo | https://docs.expo.dev |
| Expo Router | https://docs.expo.dev/router/introduction |
| Expo Location | https://docs.expo.dev/versions/latest/sdk/location |
| Expo Notifications | https://docs.expo.dev/versions/latest/sdk/notifications |
| WatermelonDB | https://nozbe.github.io/WatermelonDB |
| react-native-maps | https://github.com/react-native-maps/react-native-maps |
| EAS Build | https://docs.expo.dev/build/introduction |

### Maps
| Resource | Link |
|---|---|
| Leaflet.js | https://leafletjs.com |
| OpenStreetMap | https://www.openstreetmap.org |

### Models
| Model | Path / Access | Role in V31 |
|---|---|---|
| Claude Sonnet 4.6 (V31 execution primary) | via Claude Code | Default for Phase 4-8, Feature Updates |
| Claude Opus 4.6 (V31 escalation) | via Claude Code | Complex multi-file bugs when Sonnet pivots |
| Gemini 2.5 Flash-Lite (governance) | `google/gemini-2.5-flash-lite` | Changelog/doc writes — cheapest |
| MiniMax M2.5 (historical — V19-V30) | `minimax/minimax-m2.5` or `:free` | Was default execution via Cline; Cline deprecated V31 |
| DeepSeek V3 (historical fallback) | `deepseek/deepseek-v3` | Was execution fallback when MiniMax unavailable; no longer routed |

### Context7 library IDs (priority libs for this stack)
| Library | Context7 ID |
|---|---|
| Next.js | `/vercel/next.js` |
| Prisma | `/prisma/prisma` |
| Auth.js v5 | `/nextauthjs/next-auth` |
| tRPC | `/trpc/trpc` |
| Valkey | `/valkey-io/valkey` |
| shadcn/ui | `/shadcn-ui/ui` |
| BullMQ | `/taskforcesh/bullmq` |
| Expo | `/expo/expo` |
| WatermelonDB | `/nozbe/watermelondb` |

---

*This reference is part of the 24-file V32.12 deliverable set (18 files placed in `.ai_prompt/` + deploy script at project root + spec-executor.md → `.claude/agents/` + settings.json → `.claude/settings.json` + lint-deploy.sh → `scripts/` + design-stop-hook.sh → `scripts/` + LESSONS_REGISTRY.md → `.ai_prompt/` + privacy.md → `.ai_prompt/` + design-principles.md → `.ai_prompt/`):*
*CLAUDE_v31_compact.md · Master_Prompt_v31.md · bootstrap.md · phases.md · security.md · ui-rules.md · scenarios.md · templates.md · memory-governance.md · Product_md_Planning_Assistant_v31.md · Framework_Feature_Index_v31.md · AI_Tools_Skills_MCPs_Reference_v31.md · Post_Generation_Security_Checklist_v31.md · ChatGPT_V31_Cross_Audit_Prompt.md · Prompt_References.md · Prompt_References.html · privacy.md · design-principles.md · plus `deploy-v31.sh` at project root · plus `spec-executor.md` (→ `.claude/agents/`) and `settings.json` (→ `.claude/settings.json` merged) added at V32.7.2 · plus `lint-deploy.sh` (→ `scripts/lint-deploy.sh`, chmod +x) added at V32.7.5 · plus `privacy.md` (→ `.ai_prompt/privacy.md`) added at V32.9 · plus `design-principles.md` (→ `.ai_prompt/design-principles.md`) added at V32.12.*
